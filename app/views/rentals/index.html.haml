%h1
  = Rental.model_name.plural.humanize

%table.table.table-striped
  %thead
    %th{scope: 'col'}= Book.human_attribute_name(:title)
    %th{scope: 'col'}= t('full_name')
    %th{scope: 'col'}= Rental.human_attribute_name(:status)
    %th{scope: 'col'}= t('date')

    %tbody
      = render @rentals